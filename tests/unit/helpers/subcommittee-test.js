import { subcommittee } from 'sunlight/helpers/subcommittee';
import { module, test } from 'qunit';

module('Unit | Helper | subcommittee');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = subcommittee([42]);
  assert.ok(result);
});
